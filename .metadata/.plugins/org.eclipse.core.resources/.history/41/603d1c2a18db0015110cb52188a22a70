import java.util.Random;

// This class is going to lay the foundation for the actual game play of BlackJack

public class GamePlay {
	
	// Generates an random card
	public static int getRandomCard(Random rand){
		int card;
		
		card = rand.nextInt(10)+2;
		
		return card;
	}
	
	public static void getPlayerInput(){
		// This allows the user to pick whether to hit (get another card) or stay (dealer's turn)
		
	}

}
